{
  "name": "open-midway-examples",
  "version": "1.0.0",
  "devDependencies": {
    "git-hooks": "^1.1.10",
    "lerna": "^3.10.5",
    "lerna-relinker": "^1.4.0"
  },
  "scripts": {
    "autod": "lerna run autod",
    "test": "npm run clean && lerna run test",
    "cov": "sh scripts/cov.sh",
    "ci": "npm run build && npm run cov",
    "build": "lerna run build",
    "bootstrap": "rm -f ./packages/.DS*; lerna bootstrap --no-ci",
    "clean": "lerna clean --yes; rm -rf ./packages/**/package-lock.json; rm -rf ./packages/**/run; rm -rf ./packages/**/logs; rm -rf ./packages/**/dist; rm -rf package-lock.json; rm -rf node_modules; "
  }
}
