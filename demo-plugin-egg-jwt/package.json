{
  "name": "demo-plugin-egg-jwt",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "midway": "^1.0.0",
    "egg-jwt": "^3.1.6"
  },
  "devDependencies": {
    "midway-bin": "1",
    "midway-demo-lib": "1"
  },
  "engines": {
    "node": ">=8.9.0"
  },
  "scripts": {
    "dev": "NODE_ENV=local midway-bin dev --ts",
    "debug": "NODE_ENV=local midway-bin debug --ts",
    "test": "midway-bin test --ts",
    "cov": "midway-bin cov --ts",
    "ci": "midway-bin cov --ts",
    "build": "midway-bin build -c"
  }
}
